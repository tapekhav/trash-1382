#pragma once

enum LEVEL {
    GAME, STATUS, ERROR
};

enum OUTPUT {
    CONSOLE, FILEOUT
};

enum MOVES {
    LEFT,
    RIGHT,
    UP,
    DOWN,
    NOWHERE,
    EXIT
};

enum COMPLEXITY {
    EASY = 1,
    HARD = 2,
};